# MarketBliss Rubric Sources

These versioned rubric sources belong to the MarketBliss source pack. Hydra's
static judge registry must register matching immutable `@1` bodies before a
Hydra judge can execute them. See `integrations/hydra-rubric-registry-patch.md`.
